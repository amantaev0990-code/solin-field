/* CMF2 layer extent mapper. CarryMap stores one quantization extent per layer.
   Spatial geometry parses independently of the real-world extent, so using the first
   plausible extent for every layer makes valid shapes appear scattered. This pass
   pairs metadata extents with layers by their native order and reparses each decoded
   SpatialBlock with its own extent. */
(function(){
 function f64(v,o){try{return v.getFloat64(o,true)}catch(_){return NaN}}
 function utmToWgs84(easting,northing,zone,south=false){const a=6378137,e2=.00669438,k0=.9996,ep=e2/(1-e2);if(south)northing-=10000000;const M=northing/k0,mu=M/(a*(1-e2/4-3*e2*e2/64-5*Math.pow(e2,3)/256)),e1=(1-M.sqrt?0:(1-Math.sqrt(1-e2))/(1+Math.sqrt(1-e2))),p=mu+(3*e1/2-27*Math.pow(e1,3)/32)*Math.sin(2*mu)+(21*e1*e1/16-55*Math.pow(e1,4)/32)*Math.sin(4*mu)+(151*Math.pow(e1,3)/96)*Math.sin(6*mu),N=a/Math.sqrt(1-e2*Math.sin(p)**2),T=Math.tan(p)**2,C=ep*Math.cos(p)**2,R=a*(1-e2)/Math.pow(1-e2*Math.sin(p)**2,1.5),D=(easting-500000)/(N*k0),lat=p-(N*Math.tan(p)/R)*(D**2/2-(5+3*T+10*C-4*C*C-9*ep)*D**4/24+(61+90*T+298*C+45*T*T-252*ep-3*C*C)*D**6/720),lon=(D-(1+2*T+C)*D**3/6+(5-2*C+28*T-3*C*C+8*ep+24*T*T)*D**5/120)/Math.cos(p),origin=(zone-1)*6-180+3;return[lat*180/Math.PI,origin+lon*180/Math.PI]}
 function plausible(q){const[x0,y0,x1,y1]=q;return q.every(Number.isFinite)&&x0>100000&&x0<900000&&x1>x0&&x1<900000&&y0>100000&&y0<10000000&&y1>y0&&y1<10000000&&x1-x0>10&&y1-y0>10&&x1-x0<1000000&&y1-y0<1000000}
 function nativeExtents(view,zone,south){const out=[],seen=new Set(),limit=Math.min(view.byteLength-44,24000);for(let o=0;o<limit;o++){const q=[0,1,2,3].map(k=>f64(view,o+k*12));if(!plausible(q))continue;const key=q.map(x=>Math.round(x*100)/100).join('|');if(seen.has(key))continue;seen.add(key);const sw=utmToWgs84(q[0],q[1],zone,south),ne=utmToWgs84(q[2],q[3],zone,south);out.push({offset:o,stride:12,utm:{xmin:q[0],ymin:q[1],xmax:q[2],ymax:q[3]},wgs84:{south:Math.min(sw[0],ne[0]),west:Math.min(sw[1],ne[1]),north:Math.max(sw[0],ne[0]),east:Math.max(sw[1],ne[1])}})}return out.sort((a,b)=>a.offset-b.offset)}
 function unionExtent(exts){if(!exts.length)return null;const w=exts.map(x=>x.wgs84).filter(Boolean);if(!w.length)return exts[0];return{layerExtentUnion:true,wgs84:{south:Math.min(...w.map(x=>x.south)),west:Math.min(...w.map(x=>x.west)),north:Math.max(...w.map(x=>x.north)),east:Math.max(...w.map(x=>x.east))}}}
 async function fix(file,base){if(!base?.geometryLayers?.length||!base?.zone||!window.SolinCmf2SpatialV2)return base;const buf=await file.arrayBuffer(),raw=new Uint8Array(buf),view=new DataView(buf),exts=nativeExtents(view,base.zone,!!base.south);if(!exts.length)return base;const layerBySource=new Map((base.layers||[]).map(l=>[l.source,l])),fixed=[];for(const old of base.geometryLayers){const layer=layerBySource.get(old.source)||base.layers?.[old.index];const ext=exts[layer?.index??old.index];if(!layer||!ext||old.start==null){fixed.push(old);continue}const dec=SolinCmf2SpatialV2.decodeSpatial(raw,old.start);if(!dec?.complete){fixed.push(old);continue}const g=SolinCmf2SpatialV2.parse(dec.bytes,layer,ext,base.zone,!!base.south);if(!g){fixed.push(old);continue}fixed.push({...g,start:old.start,inputEnd:dec.inputEnd,compressedBytes:dec.inputLength,logicalBytes:dec.outputLength,boundaries:dec.boundaries,extent:ext,layerExtentMapped:true})}if(!fixed.length)return base;base.geometryLayers=fixed;base.decodedFeatures=fixed.reduce((s,g)=>s+(g.count||0),0);base.decodedVertices=fixed.reduce((s,g)=>s+(g.totalVertices||0),0);base.layerExtents=exts;base.extent=unionExtent(fixed.map(g=>g.extent).filter(Boolean))||base.extent;base.layerExtentMapping=true;return base}
 function install(){if(typeof window.inspectCmf2!=='function'||window.inspectCmf2._layerExtentMapped)return false;const orig=window.inspectCmf2;const wrapped=async file=>fix(file,await orig(file));wrapped._layerExtentMapped=true;window.inspectCmf2=wrapped;return true}if(!install())setTimeout(install,0);
 window.SolinCmf2LayerExtents={nativeExtents};
})();